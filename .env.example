# Application log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Directory where trained artifacts (model.pth, scaler.joblib, anomaly_threshold.npy) are stored
ARTIFACT_DIR=artifacts

# Port the uvicorn server listens on (used by docker-compose)
PORT=8000
